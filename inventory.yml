all:
  children:
    linux:
      vars:
        ansible_ssh_user: docker
        ansible_ssh_pass: password
        ansible_port: 22
        ansible_ssh_private_key_file:
      children:
        mariadb:
          hosts:
            127.0.0.1:
        passbolt:
          hosts:
            127.0.0.1:
